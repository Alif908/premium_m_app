name: premium_m_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.2.3
  mobile_scanner: ^5.2.3
  image_picker: ^1.2.2
  razorpay_flutter: ^1.4.5
  geolocator: ^11.0.0
  flutter_local_notifications: ^22.0.0
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  url_launcher: ^6.3.2
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/logo/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo/bada store pr.jpg"